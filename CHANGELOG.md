# Changelog

## Unreleased

- Rebuilt persistence around SQLite raw messages, derived memories, explicit edges, and schema migrations.
- Added normalized exact cosine retrieval, independent three-signal scoring, post-seed affinity, and inspectable reasons.
- Added deterministic hard-budget context assembly with provenance and transactional inclusion accounting.
- Replaced the primary proxy runtime with an Anthropic-compatible store-backed engine and safe fallback behavior.
- Added redaction, source-aware trust, supersession, deletion controls, evaluation baselines, ablations, and latency benchmarks.
- Archived the earlier custom tree experiment and removed it from the installed runtime.

